<div class="withheld_taxes">
  <div>
    <span><%= @list.first.valid_from %></span>
    <span>～</span>
    <span><%= @list.first.valid_until %></span>
  </div>
  <table class="table table-striped table-hover">
    <tr>
      <th>社会保険料控除後の<br/>給与範囲（以上）</th>
      <th>社会保険料控除後の<br/>給与範囲（未満）</th>
      <th>扶養親族<br/>０人</th>
      <th>扶養親族<br/>１人</th>
      <th>扶養親族<br/>２人</th>
      <th>扶養親族<br/>３人</th>
      <th>扶養親族<br/>４人</th>
      <th>扶養親族<br/>５人</th>
      <th>扶養親族<br/>６人</th>
      <th>扶養親族<br/>７人</th>
      <th>乙</th>
    </tr>
    <% @list.each do |data| %>
      <tr>
        <td class="salary_range"><%= to_amount(data.salary_range_from, :show_zero => true) %></td>
        <td class="salary_range"><%= to_amount(data.salary_range_to, :show_zero => true) %></td>
        <% 8.times do |i| %>
          <td><%= to_amount(data.__send__("dependent_#{i}"), :show_zero => true) %></td>
        <% end %>
        <td><%= to_amount(data.sub_salary, :show_zero => true) %></td>
      </tr>
    <% end %>
  </table>
</div>
