<%= render 'form' %>
<script>
  var form = $('form[action="/mm/employees/<%= @e.id %>"]');
  form.find('input, select').attr('disabled', true);
  form.find('img').removeAttr('onclick');
  form.find('a').removeAttr('onclick').on('click', (e) => { e.preventDefault(); });
</script>
