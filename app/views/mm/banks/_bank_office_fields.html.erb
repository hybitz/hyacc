<%= fields_for "bank[bank_offices_attributes]", bank_office, :index => index do |f| %>
  <tr class="bank_office">
    <td class="code">
      <% if f.object.new_record? %>
        <%= f.text_field :code %>
      <% else %>
        <%= f.hidden_field :id %>
        <%= f.object.code %>
      <% end %>
    </td>
    <td class="name"><%= f.text_field :name %></td>
    <td class="address"><%= f.text_field :address %></td>
    <td><%= f.select :disabled, finder.disable_types %></td>
  </tr>
<% end %>

