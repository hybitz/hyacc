<%#
# $Id: index.html.erb 3295 2014-01-24 01:03:37Z ichy $
# Product: hyacc
# Copyright 2009-2014 by Hybitz.co.ltd
# ALL Rights Reserved.
%>
<%= flash_notice %>

<table>
  <tr>
    <th>ログインID</th>
    <th>氏名</th>
    <th>状態</th>
    <th colspan="3"></th>
  </tr>

  <% @users.each do |user| %>
    <tr>
      <td><%= user.login_id %></td>
      <td><%= user.employee.full_name %></td>
      <td><%= HyaccConstants::DELETED_TYPES[user.deleted] %></td>
      <td><%= link_to '参照', {:action => 'show', :id => user.id}, :remote => true %></td>
      <td><%= link_to '編集', {:action => 'edit', :id => user.id}, :remote => true %></td>
      <td><%= link_to '削除', {:action => 'destroy', :id => user}, :confirm => '削除します。よろしいですか？', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<%= will_paginate @users %>
<br/>
<%= link_to '追加', {:action => 'new'}, :remote => true %>

