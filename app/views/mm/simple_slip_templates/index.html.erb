<%= render 'search' %>
<%= flash_notice %>

<% if @templates.empty? %>
  <div>登録されているテンプレートはありません。</div>
<% else %>
  <%= render 'index' %>
<% end %>

<%= will_paginate @templates if @templates.present? %>
<%= link_to '追加', new_mm_simple_slip_template_path(account_id: finder.account_id, remarks: finder.remarks), class: 'btn btn-light add', remote: true %>
