var notification_dialog = hyacc.current_dialog({title: 'お知らせ'});

if (window.location.pathname === '<%= root_path %>') {
  notification_dialog.close = function() {
    location.reload();
   }
}

notification_dialog.show('<%=j(render 'index')%>');
