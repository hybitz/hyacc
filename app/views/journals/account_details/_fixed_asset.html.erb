<%= fields_for "journal_details[#{index}]", jd do |f| %>
  <% if jd.asset and jd.asset.persisted? %>
    資産コード：<%= jd.asset.code %>

    <%= f.hidden_field :asset_id, :value => jd.asset.id %>
    <%= f.hidden_field :asset_lock_version, :value => jd.asset.lock_version %>
  <% end %>
<% end %>
