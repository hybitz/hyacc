<%= flash_notice %>

<div class="m-2 col-md-6">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th><%= DonationRecipient.human_attribute_name :kind %></th>
        <th><%= DonationRecipient.human_attribute_name :name %></th>
        <th class="text-center text-nowrap"></th>
      </tr>
    </thead>
    <tbody>
      <% @donation_recipients.each do |dr| %>
        <tr>
          <td class="align-middle text-nowrap"><%= donation_recipient_kind_label(dr.kind) %></td>
          <td class="align-middle"><%= dr.name %></td>
          <td class="text-center text-nowrap">
            <%= link_to '参照', mm_donation_recipient_path(dr), remote: true, class: 'btn btn-sm btn-light' %>
            <%= link_to '編集', edit_mm_donation_recipient_path(dr), remote: true, class: 'btn btn-sm btn-light' %>
            <%= link_to '削除', mm_donation_recipient_path(dr), method: :delete, class: 'btn btn-sm btn-light', data: { confirm: "寄付先「#{dr.name}」を削除します。\nよろしいですか？" } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= will_paginate @donation_recipients %>
  <%= link_to '追加', new_mm_donation_recipient_path, class: 'btn btn-light add', remote: true %>
</div>
