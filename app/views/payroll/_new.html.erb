<%= flash_notice %>
<hr/>
<b>給与明細</b>
</br>
<%= form_tag({:action => 'create'}, :id => 'payroll_form', :remote => true) do %>
  <%= render 'form' %>
  <table width="400">
    <tr>
      <td colspan="2">
        <%= submit_tag '登録', :id => 'payroll_submit', :onclick => "return checkForm(this.form)" %>
      </td>
    </tr>
  </table>
<% end %>